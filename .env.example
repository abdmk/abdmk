# Password for /admin. REQUIRED in production — without it every login is
# refused. In development, any password is accepted when this is unset.
ADMIN_PASSWORD=

# Key used to sign the admin session cookie. Set this to a long random string
# in production. Falls back to ADMIN_PASSWORD when unset.
ADMIN_SECRET=
